## Setup
To start bootstrap, run:

```
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/fl4shback/dotfiles/HEAD/bootstrap.sh)"
```
## Backup
To generate backup folder to copy to new computer, run backup.sh
